.link_container
  = link_to questions_path do 
    .content_right_link
      %p 質問部屋へ行く
  = link_to populars_path do 
    .content_right_link.mt-2
      %p 人気投稿を見る
  - if user_signed_in?
    - if current_page?(controller: 'timelines')
      = link_to root_path do
        .content_right_link.mt-2
          %p すべての投稿を見る
    - else
      = link_to timelines_path do 
        .content_right_link.mt-2
          %p フォローしているユーザーの投稿

.search_form.mb-3
  %p.font-weight-bold 検索ワードで探す
  = form_tag(search_index_path,:method => 'get') do
    = text_field_tag :search, '', class: 'form w-100'
    = submit_tag '検索', :name => nil, class: 'btn submit_btn search_btn w-100 mt-2'

= render 'commons/prefecture'

.tag_wrap
  %p.font-weight-bold タグで探す
  - @tag_lists.each do |list|
    = link_to list.tag_name, tags_path(tag_id: list.id), class: 'tag_list'
  %div
    = link_to 'タグ一覧へ', taglists_path, class: 'tag_index_link'
