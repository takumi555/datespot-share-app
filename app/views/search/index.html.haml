.container
  .content_right
    = render 'commons/content_right'

  .content_left
    .font-weight-bold
      = "「 #{@search} 」の検索結果" 
    - @posts.each do |post|
      = render 'commons/posts', post: post
    = paginate @posts