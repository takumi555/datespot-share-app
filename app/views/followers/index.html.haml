.container
  %p.follow_title フォロワー
  - @followers.each do |follower|
    .follow_list
      .follow_user_info
        - if follower.id == current_user.id
          = link_to profile_path do
            = image_tag follower.avatar_image, class: 'follow_list_avatar'
            %p.follow_name= follower.username
        - else
          = link_to account_path(follower.id) do
            = image_tag follower.avatar_image, class: 'follow_list_avatar'
            %p.follow_name= follower.username
        